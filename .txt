/* ============================================================
   ABOUT - CORREGIDO CON ANIMACIONES ESCALONADAS
   ============================================================ */

/* 1. DEFINICIÓN DE LA ANIMACIÓN (Agregado) */
@keyframes fadeInUp {
  from {
    opacity: 0;transform: translateY(30px); /* Empieza 30px abajo */
  }
  to {opacity: 1;transform: translateY(0); /* Termina en su posición original */
  }
}

.about { border-top: 1px solid var(--border); }

.about-grid {
  display: grid;
  grid-template-columns: 280px 1fr;
  gap: 60px;
  align-items: start;
  background: transparent;
}

/* --- SECTOR DE LA FOTO (Caja Izquierda) --- */
.about-avatar-col {
  background: var(--bg-2);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 30px;
  display: flex;
  flex-direction: column;
  align-items: center;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
  
  /* ANIMACIÓN: Aparece primero */
  animation: fadeInUp 0.8s ease-out forwards;
}

.avatar-frame {
  width: 180px; height: 180px;
  border-radius: 50%;
  border: 2px solid var(--accent);
  overflow: hidden;
  margin-bottom: 24px;
  position: relative;
  box-shadow: 0 0 15px rgba(0, 240, 192, 0.2);
}
.avatar-frame img { width: 100%; height: 100%; object-fit: cover; }

.about-meta { width: 100%; display: flex; flex-direction: column; gap: 15px; }
.meta-row { display: flex; flex-direction: column; gap: 4px; border-bottom: 1px solid rgba(255, 255, 255, 0.05); padding-bottom: 8px; }
.meta-row:last-child { border-bottom: none; }
.meta-key { font-size: 10px; letter-spacing: 0.12em; color: var(--text-muted); text-transform: uppercase; }
.meta-val { font-size: 13px; color: var(--text-bright); font-family: var(--font-mono); }

.status-online { 
  color: var(--accent);
  font-weight: bold;
  display: flex;
  align-items: center;
  gap: 5px;
}
.status-online::before {
  content: "";
  width: 8px; height: 8px;
  background: var(--accent);
  border-radius: 50%;
  display: inline-block;
  animation: blink 1.5s infinite;
}

@keyframes blink { 0% { opacity: 1; } 50% { opacity: 0.3; } 100% { opacity: 1; } }

/* --- SECTOR TERMINAL (Caja Derecha Arriba) --- */
.about-terminal {
  background: var(--bg-2);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 32px;
  margin-bottom: 25px;
  
  /* ANIMACIÓN: Aparece con un pequeño retraso (0.2s) */
  opacity: 0; 
  animation: fadeInUp 0.8s ease-out 0.2s forwards;
}

.terminal-line { display: flex; gap: 10px; margin-bottom: 8px; }
.prompt { color: var(--accent); }
.cmd { color: var(--text-bright); }
.terminal-output { color: var(--text-muted); margin-bottom: 28px; padding-left: 20px; font-size: 13px; }

/* --- SECTOR STATS (Cajas de Abajo) --- */
.about-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
}

.stat { 
  background: var(--bg-2);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 25px;
  text-align: center;
  transition: all 0.3s ease;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  
  /* ANIMACIÓN: Empiezan invisibles y suben en cascada */
  opacity: 0;
}

/* Aplicación de animación individual para efecto cascada */
.stat:nth-child(1) { animation: fadeInUp 0.8s ease-out 0.4s forwards; }
.stat:nth-child(2) { animation: fadeInUp 0.8s ease-out 0.5s forwards; }
.stat:nth-child(3) { animation: fadeInUp 0.8s ease-out 0.6s forwards; }

.stat:hover {
  border-color: var(--accent);
  transform: translateY(-5px);
  box-shadow: 0 10px 20px rgba(0, 240, 192, 0.1);
}

.stat-num { font-family: var(--display); font-size: 2.2rem; font-weight: 800; color: var(--accent); line-height: 1; margin-bottom: 5px; }
.stat-label { font-size: 10px; letter-spacing: 0.15em; color: var(--text-muted); text-transform: uppercase; }
